if (typeof Minionfo === "undefined") {
    var Minionfo= {
    	Init: function(){
    		$(document).ready(function(){
    			$("#success-msg").slideDown(400);
    		});
    	}
    };
    Minionfo.Init();
}
